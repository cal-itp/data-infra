version: 2
exposures:
  - name: invalid_ride_vault_ids
    label: Invalid Ride Vault IDs
    description: "### Visualization: Table\n\nNo description provided in Metabase\n\
      \n#### Query\n\n```\nselect\n    participant_id,\n    micropayment_id,\n   \
      \ r.funding_source_vault_id,\n    r.customer_id,\n    v.customer_id,\n    r.principal_customer_id,\n\
      \    v.principal_customer_id,\n    r.product_code,\n    v.bin,\n    v.masked_pan,\n\
      \    v.issuer,\n    v.form_factor\nfrom views.payments_rides as r\njoin payments.stg_cleaned_customer_funding_source_vaults\
      \ as v\n    on r.funding_source_vault_id = v.funding_source_vault_id\n    and\
      \ timestamp(r.transaction_date_time_utc) between v.calitp_valid_at and (v.calitp_invalid_at\
      \ - interval 1 second)\nwhere r.customer_id not in (v.customer_id, v.principal_customer_id)\n\
      and r.principal_customer_id not in (v.customer_id, v.principal_customer_id)\n\
      ```\n\n#### Metadata\n\nMetabase ID: __246__\n\nCreated On: __2021-12-23T05:54:57.922542Z__"
    type: analysis
    url: https://dashboards.calitp.org/card/246
    maturity: medium
    owner:
      name: Mjumbe Poe
      email: mjumbe.p@jarv.us
    depends_on: []
    config:
      meta:
        last_used_at: '2024-09-20T17:54:54.131902Z'
