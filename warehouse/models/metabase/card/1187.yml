version: 2
exposures:
  - name: current_organizations_offering_fixed_route_service__not_all_orgs_are_undergoing_checks_
    label: Current Organizations offering Fixed Route Service (not all orgs are undergoing
      checks)
    description: '### Visualization: Scalar


      No description provided in Metabase


      #### Query


      ```

      SELECT `mart_transit_database.dim_services`.`key` AS `key`,

      `mart_transit_database.dim_services`.`name` AS `name`,

      `mart_transit_database.dim_services`.`source_record_id` AS `source_record_id`,

      `mart_transit_database.dim_services`.`service_type` AS `service_type`,

      `mart_transit_database.dim_services`.`mode` AS `mode`,

      `mart_transit_database.dim_services`.`_valid_to` AS `_valid_to`,

      `mart_transit_database.dim_services`.`_is_current` AS `_is_current`

      FROM `mart_transit_database.dim_services`

      WHERE  (''fixed-route'' IN UNNEST(service_type) )

      ```


      #### Metadata


      Metabase ID: __1187__


      Created On: __2023-03-03T01:38:31.106306Z__'
    type: analysis
    url: https://dashboards.calitp.org/card/1187
    maturity: medium
    owner:
      name: Julia Ma
      email: julia.ma@dot.ca.gov
    depends_on: []
    config:
      meta:
        last_used_at: '2024-09-20T17:54:54.131902Z'
