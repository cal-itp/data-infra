apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql-backup.fullname" . }}
  labels:
    {{- include "postgresql-backup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgresql-backup.selectorLabels" . | nindent 4 }}
