apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresql-backup.fullname" . }}-test-connection"
  labels:
    {{- include "postgresql-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresql-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
